{
  "name": "@got/api-tests",
  "version": "0.1.0",
  "description": "API tests for got graph database",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "build": "bun build index.ts --outdir=dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest"
  },
  "dependencies": {
    "@got/api-spec": "workspace:*",
    "@got/util": "workspace:*"
  }
}
