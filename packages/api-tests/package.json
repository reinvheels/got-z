{
  "name": "@got-z/api-tests",
  "version": "0.1.0",
  "description": "API tests for Got-Z graph database",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "build": "bun build index.ts --outdir=dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "bun-types": "latest",
    "@types/node": "latest"
  },
  "dependencies": {
    "@got-z/api-spec": "workspace:*"
  }
}