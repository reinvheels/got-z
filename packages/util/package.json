{
  "name": "@got-z/util",
  "version": "0.1.0",
  "description": "Utility functions for Got-Z graph database",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "bun build index.ts --outdir=dist",
    "dev": "bun run --watch index.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest"
  },
  "dependencies": {}
}
