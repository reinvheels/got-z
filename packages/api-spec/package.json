{
  "name": "@got/api-spec",
  "version": "0.1.0",
  "description": "API specification for Got graph database",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "test": "bun run build",
    "build": "bun build src/index.ts --outdir=dist",
    "dev": "bun run --watch src/index.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest"
  },
  "dependencies": {
    "zod": "^3.22.4"
  }
}
